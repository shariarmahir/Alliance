@AGENTS.md

# AutoLink Integrated Technologies — Project Guide

AutoLink Integrated Technologies (brand wordmark: "AutoLink") is a premium B2B
e-commerce storefront for industrial electronics (PLCs, inverters, servos, motors,
power system electronics, factory/garment automation parts), based in Bangladesh but
built to be sold and shipped internationally. UI/UX is cloned from Radwell
(radwell.co.uk) for the landing page, product listing, and product detail screens.
This is a **quotation-based B2B flow**, not a cart/checkout store: there is no
"Add to Cart" — customers request a quotation, confirm an order from that quote, and
track delivery.

Alongside the storefront sits a full **Super Admin / Sub-Admin dashboard** for running
the business: catalog management, order/quotation operations, employee/task/leave
management, and analytics.

> Note: the repo folder and git remote are still named `Alliance` /
> `Alliance-frontend` (pre-rebrand). The product itself, all user-facing text, and all
> internal identifiers (cookie name, storage keys, email domain) were renamed to
> AutoLink — see "Naming History" below before assuming any `alliance`-prefixed string
> still exists in app code.

## Stack

- Next.js 16 (App Router, Turbopack), React 19, TypeScript
- Tailwind CSS v4 (`@theme` tokens in `app/globals.css`, no `tailwind.config.js`)
- shadcn/ui components (Base UI primitives, not Radix) in `app/components/ui/`
- Recharts for admin analytics charts
- **All data comes from the FastAPI backend in `Allaince-backend/`** (sibling folder,
  one level up) over HTTP. This app has no database, no `app/api/**` route handlers,
  and no JSON persistence — those were removed once the backend took over. See
  "Data Layer" below.

**Read `node_modules/next/dist/docs/` before writing App Router code that touches
routing, layouts, or server/client boundaries** — this Next.js version has breaking
changes from what you may expect (`middleware.ts` is renamed `proxy.ts`, for example).
See `AGENTS.md` for the enforcement note; that file is regenerated by `next dev` and
should be committed as-is, not hand-edited.

## Repository Layout

```
Alliance/                  ← repo root folder name (legacy, unchanged)
├── Alliance-frontend/     ← this app (see below) — folder name also legacy/unchanged
├── Allaince-backend/      ← FastAPI + PostgreSQL backend (folder-name typo is
│                             pre-existing and deliberate; see its README)
└── docs/superpowers/      ← design specs and implementation plans for each phase
```

Run both in development: the backend on `:8000` (`uvicorn app.main:app --reload`) and
this app on `:3000`. Without the backend up, every page renders its empty state.

Everything in this file is scoped to `Alliance-frontend/`.

## Conventions (non-negotiable, per project owner)

- **No `src/` folder.** Everything lives directly under `app/`.
- **One file per route: `page.tsx`.** Route logic and markup live in `page.tsx`
  (server component) with a `*-client.tsx` sibling only when interactivity requires
  `"use client"`. Don't fragment a single screen across unnecessary extra files.
- **Global CSS only.** All styling is Tailwind utility classes plus the tokens/utilities
  in `app/globals.css` (see `.btn-glass`, `.btn-glass-accent`, `.glass-panel`). No
  per-page CSS Modules, no styled-components, no inline `<style>` blocks.
- **Short, direct code.** No speculative abstractions, no unused config toggles, no
  defensive code for cases that can't occur server-side. Prefer three similar lines
  over a premature helper.
- Server-only data access (`fs`/`fs/promises`) is isolated behind modules that
  `import "server-only"` at the top — never imported into a `"use client"` file
  directly. If a client component needs that data, the server component (`page.tsx`)
  fetches it and passes it down as a prop. See "Data Layer" below.

## Theme

- Background: white (light mode only, no dark mode toggle currently)
- Primary: `#007DCC` (blue) — `bg-primary` / `text-primary` via the `--color-primary`
  token in `app/globals.css`. Used for the "AutoLink" wordmark text.
- Accent/CTA: `#FFB900` (orange/gold) — `bg-accent` / `text-accent`. Also used for the
  `.` after "AutoLink" in the wordmark (header + footer).
- Buttons use glassmorphism + shine-sweep animation: `.btn-glass` (primary) and
  `.btn-glass-accent` (accent), defined once in `globals.css`. Use these classes for
  all primary CTAs instead of ad hoc button styling.
- Font: Inter (loaded via Google Fonts `@import` in `globals.css`), plus
  `Geist`/`Geist_Mono` variables wired in `app/layout.tsx` for shadcn defaults.

## Logo

- Source mark: `public/Autolink-1.png` (as supplied — square canvas, padded).
- `public/logo.png` — regenerated from the source with the white background knocked
  out to transparency (source is `RGB`, not `RGBA`). 512px square canvas, ~8% padding.
- `public/logo-mark.png` — tightly cropped to the mark's true content bounding box (no
  canvas padding). **This is the one actually used in components** — the padded
  `logo.png` renders too small/disconnected next to the wordmark text at fixed sizes.
- Used in: `app/components/header-client.tsx` (storefront header, 44px),
  `app/admin/admin-sidebar.tsx` (32px), `app/admin/login/page.tsx` (56px).
- If the source logo file is ever replaced, regenerate both derived files (transparency
  knockout + bounding-box crop) rather than pointing components at the raw upload —
  see git history around the rebrand commits for the exact Pillow script used.

## Route Map

### Storefront (`app/(site)/`)

| Route | Purpose |
|---|---|
| `/` | Landing page: hero carousel, category grid, top sellers, services, brand strip, client reviews, "We Are Here To Help You" contact panel |
| `/products` | Product listing with category/brand filters (Radwell-style) |
| `/products/[slug]` | Product detail — spec table, gallery, **"Request Quotation"** button (not Add to Cart) |
| `/quote` | Quotation cart — quantities/pricing for all requested items, submits to `/api/quotations` |
| `/order/confirm` | Delivery options + date picker after a quotation is accepted |
| `/order/success` | Order number, tracking ID/link, invoice download (jsPDF) and print |
| `/track/[trackingId]` | Live delivery tracking timeline |
| `/contact` | Real contact form, posts to `/api/contact` |

### Admin (`app/admin/`)

Auth-gated by `proxy.ts` (this Next version's renamed `middleware.ts`). Session cookie
is `autolink_admin_session`, parsed by `parseAdminSession()` in `app/lib/admin-auth.ts`.
Shape: `{ role: "super" | "sub", name, email, employeeId? }`.

- `/admin/login` — credential form (see Auth & Accounts below)
- `/admin` — role-branched: super admin sees the analytics Overview; sub-admin sees
  their personal dashboard (`sub-admin-dashboard.tsx`)
- Super-admin-only: `/admin/orders`, `/admin/quotations`, `/admin/contact-requests`,
  `/admin/emails`, `/admin/employees`
- Shared (super + sub, per RBAC below): `/admin/products`, `/admin/stock`,
  `/admin/hero-images`, `/admin/tasks`, `/admin/leave`, `/admin/daily-report`

**RBAC is enforced in the backend.** `proxy.ts` is now only a navigation gate: it keeps
a sub-admin from landing on a page that would render nothing but 403s. Being wrong
there degrades the UI; it does not expose data, because the backend rejects an
unauthorised request regardless of which UI made it.

`SUB_ADMIN_ALLOWED_PREFIXES` (prefix match) plus `SUB_ADMIN_ALLOWED_EXACT = ["/admin"]`
(exact only — `/admin` must never become a prefix match or it would swallow every
super-only `/admin/*` route) still drive those redirects. Add a new sub-admin route to
the prefix list; add nothing for a super-only route, which is blocked by default.

The real rules live in `Allaince-backend/app/core/deps.py` — `require_admin`,
`require_area(area)`, `require_super_admin`, plus `owns_or_super()` for per-resource
ownership (a sub-admin may progress their own task but gets 403 on a colleague's).

## Auth & Accounts

Credential checking, bcrypt hashing and login rate limiting all live in the backend.
`loginAction` in `app/admin/login/actions.ts` posts to `POST /api/admin/login`, then
copies the signed token out of the backend's `Set-Cookie` onto this app's own origin
(the backend's cookie is scoped to the API origin, which the browser will not send
back here).

Accounts come from the backend's `employees` table, plus one bootstrap super admin
configured through its environment (`SUPER_ADMIN_EMAIL` /
`SUPER_ADMIN_PASSWORD_HASH_B64`). The previously hardcoded mock accounts are gone.

Both roles land on `/admin`; the page branches on `session.role`.

## Data Layer

Everything is fetched from the FastAPI backend over HTTP. There is no database, no
`data/*.json` persistence and no `app/api/**` in this app any more.

| Module | Use from | Covers |
|---|---|---|
| `app/lib/api-client.ts` | server components, server actions | Low-level `api.get/post/patch/delete`, `getOrNull`, `getOrDefault`. Forwards the session cookie when called with `auth: true`. |
| `app/lib/api-browser.ts` | `"use client"` components | `apiFetch`, `apiUpload` (multipart), `apiDownload` (PDFs). Sends `credentials: "include"`. |
| `app/lib/catalog-data.ts` | storefront | Products, categories, brands, hero images, top sellers, tracking, quotation submit |
| `app/lib/admin-data.ts` | admin screens | Analytics, orders, quotations, employees, tasks, leave, reports, search, Gmail |
| `app/lib/static-content.ts` | anywhere | `reviews` and `faqs` — editorial copy, deliberately not backend data |

`API_URL` (server-side) and `NEXT_PUBLIC_API_URL` (browser) point at the backend.

**`SESSION_SECRET` must be byte-identical to the backend's.** `proxy.ts` verifies the
session JWT locally to decide redirects, so a mismatch means every backend-issued
token is rejected and the whole admin area redirects to `/admin/login` with no error
in any log — the exact symptom is a 307 on every `/admin/*` page.

Admin reads return `[]` when the caller lacks the grant (see `getOrDefault`), so a
sub-admin's dashboard renders empty sections instead of failing the page. Real
authorisation is the backend's job — it 403s regardless of which UI called it.

`app/lib/mock-analytics.ts` still holds generated data for a few secondary charts
(traffic sources, clients by country); the KPI cards and revenue chart are real.

### Server-only import boundary (recurring gotcha)

`api-client.ts` uses `next/headers`, so it only works in server components and server
actions. A `"use client"` component must use `api-browser.ts`, or take the data as a
prop from a server parent. Example: `most-requested-parts.tsx` (server, fetches all
three periods) renders `most-requested-parts-tabs.tsx` (`"use client"`, receives them
as props) so switching tabs needs no round trip.

### `useSearchParams()` requires Suspense

Next's static prerendering fails at **build time only** (not caught by `tsc` or
`eslint`) if a page calls `useSearchParams()` without a `<Suspense>` boundary above it.
Pattern used in `app/(site)/order/success/page.tsx`: thin default-export wrapper
renders `<Suspense fallback={null}><PageContent /></Suspense>`, and the component
calling `useSearchParams()` is the inner `PageContent`, not the export. Always run a
real `npm run build` before pushing — `tsc --noEmit` and `next lint` both pass while
this error still breaks production/Vercel deploys.

## Category & Brand Cards (design pattern)

`app/components/category-grid.tsx` and `app/components/brand-strip.tsx` use a small
rotating palette of Tailwind gradient/ring theme objects (indexed by array position,
`% themes.length`) to give each card a distinct hover treatment without needing
per-category background images or external assets. Follow this pattern rather than
sourcing/hardcoding photography for new card grids.

## Working Directory Gotcha

`package.json` lives in `Alliance-frontend/`, **not** the repo root
(`M:\Private projects\Alliance\`). Always `cd` into `Alliance-frontend` before running
`npm run dev`, `npm run build`, etc. — running them from the repo root fails with
`ENOENT: no such file or directory, open 'package.json'`. The same root-folder
mismatch was the cause of an early Vercel deployment 404: the Vercel project's **Root
Directory** setting must be `Alliance-frontend`, or Vercel builds from the repo root
and finds no app.

## Commands

```bash
cd Alliance-frontend
npm run dev          # http://localhost:3000
npm run build         # full production build — run this before every push
npx tsc --noEmit       # type-check only (faster, but does NOT catch build-only errors like missing Suspense)
npm run lint
```

## Deployment

- Git remote: `github.com/shariarmahir/Alliance.git`, single branch: `master`
  (production branch on Vercel). No `feature/*` branches should exist on the remote —
  all work merges directly to `master` after verification.
- Vercel project **Root Directory** must be `Alliance-frontend`.
- Before every push: `npx tsc --noEmit` → `npm run lint` → `npm run build` (all three,
  in that order — the production build catches errors the first two don't).

## Footer / Contact Constants

Reused across the footer, WhatsApp popup widget, and the homepage contact panel:
- Address: House: 104, Road: 15, Sector: 11, Uttara, Dhaka-1230, Bangladesh
- Email: `info@auto-bd.com`
- Phone / WhatsApp: `+8801315-770099`
- Copyright: "© AutoLink Integrated Technologies 2026–2028. All rights reserved.
  Developed by Mahir Shariar Mahin."

## Naming History

The product was renamed from "Alliance" to "AutoLink Integrated Technologies"
(wordmark: "AutoLink") mid-build. Everything user-facing and every internal identifier
was updated at that point:
- Session cookie: `alliance_admin_session` → `autolink_admin_session`
- localStorage keys: `alliance_order`/`alliance_quote`/`alliance_quotation` →
  `autolink_order`/`autolink_quote`/`autolink_quotation`
- Contact email: `info@alliance.com` → `info@autolink.com`
- `package.json` name: `alliance-frontend` → `autolink-frontend`

**What was deliberately left unchanged:** the repo root folder (`Alliance/`), the
`Alliance-frontend/` app folder itself, and the GitHub remote
(`github.com/shariarmahir/Alliance.git`) — renaming those is a filesystem/git-remote
operation with real risk (breaks the working directory path, local clones, CI/deploy
hooks pointed at the old remote name) and hasn't been requested. Don't assume the repo
folder name reflects the current brand. Also left alone: any pre-existing mock data
referencing unrelated third-party company names (e.g. a vendor called "Alliance
Freight Forwarders" in `data/emails.json`'s sample inbox) — that's a different
business, not this one, and renaming it would be a data-accuracy bug, not a fix.

## Design Specs & Plans

Each major phase of this build has a written spec and implementation plan under
`docs/superpowers/` (one level above `Alliance-frontend/`, at the repo root):
`docs/superpowers/specs/*-design.md` and `docs/superpowers/plans/*.md`. Check these
before making structural changes to a system they cover (catalog, operations, employee
system) — they document the reasoning behind decisions like the JSON-file persistence
model and the two-layer RBAC split, not just the resulting code. Note these were
written before the AutoLink rename and may still refer to the product as "Alliance"
internally — that's a naming artifact, not a scope difference.
